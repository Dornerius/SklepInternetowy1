@model IEnumerable<SklepData.Data.CMS.Page>

<nav class="light-blue" role="navigation">
    <div class="nav-wrapper container">

        <a id="logo-container" href="#" class="brand-logo light-blue">
            <img src="~/images/logo.png" alt="Logo Sklepu" style="height: 50px; margin-right: 10px; vertical-align: middle;">
            TwójSklep.pl
        </a>
        <ul class="right hide-on-med-and-down">
            <li>
                <a class="nav-link text-dark" asp-controller="Index">Strona główna</a>
            </li>
            @foreach (var item in Model)

            {
                <li>
                    <a class="nav-link text-dark" asp-controller="Home" asp-action="Index" asp-route-id="@item.IdPage">@item.LinkTitle</a>
                </li>
            }
            <li>
                <a class="nav-link text-dark" asp-controller="Index">Strona główna</a>
            </li>
            @foreach (var item in Model)

            {
                <li>
                    <a class="nav-link text-dark" asp-controller="News" asp-action="Index" asp-route-id="@item.IdPage">@item.LinkTitle</a>
                </li>
            }
        </ul>
        <a href="#" data-activates="nav-mobile" class="button-collapse"><i class="material-icons">menu</i></a>
    </div>
</nav>